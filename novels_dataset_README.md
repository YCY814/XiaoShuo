# 小说数据集

爬虫采集的小说元数据，供前端直接使用。数据文件：**`novels.csv`**。

## 概览

| 项目 | 数量 |
|---|---|
| 小说总数 | 198 本 |
| 分类 | 其他 36 / 玄幻 35 / 历史 29 / 都市 28 / 武侠 20 / 言情 18 / 科幻 17 / 悬疑 15 |
| 真实评分 | **16 本** |
| 评分为填充值 | **182 本** |
| 有作者 | 105 本（93 本作者为空） |
| 有封面图 | 158 本（封面 URL 不在本 CSV 里，见下） |

## 字段

按 `database.txt` 的约定：UTF-8 编码、英文逗号分隔、第一行英文表头。

| 字段 | 说明 | 长度 |
|---|---|---|
| `title` | 书名 | ≤100 |
| `author` | 作者，抓不到时为空字符串 | ≤50 |
| `category` | 分类：玄幻/其他/历史/都市/武侠/言情/悬疑/科幻 | ≤20 |
| `rating` | 评分，保留 1 位小数 | — |
| `description` | 简介 | ≤200 |
| `url` | 来源页面链接 | ≤500 |

## rating 有 182 本是填充值，别当真

这是这份数据最大的坑，**按 rating 排序前请先看这段**。

采集源（公开榜单页）绝大多数不给结构化评分，198 本里只有 16 本抓到了真实评分。
剩下 182 本的 `rating` 是**按同分类均值填的占位值**，不是真实评分：

- 各分类均值（只采纳 ≥5 分的真实评分计算，页面上的无关数字会被正则误抓成 1.0、4.6 这类值，不参与计算）：历史 8.3、悬疑 8.5、玄幻 9.0、言情 8.6
- 都市、科幻、其他这三个分类一本真实评分都没有，全部填的是全局均值 **8.7**
- 也就是说：**所有 rating = 8.7 的书，评分都是假的**

真要按评分排序，只能在那 16 本真实评分里排；或者干脆用榜单顺序。

## 其他已知的数据缺陷

- **作者缺失 93 本**：榜单页作者栏经常填成评论人或干脆空着。
- **简介质量参差**：部分简介来自榜单页正文，可能残留"【推荐指数】🌟🌟🌟"这类模板词，
  个别条目甚至抓到了页面导航栏（如"邮箱：xxx@lifeweek.com.cn"）。用之前建议抽查。
- **同一本书可能有重复条目**：不同榜单页给的译名不同，比如
  《2001：太空漫游》/《2001太空漫游》、《狼廳》/《Wolf Hall》、
  《The Known World》/《已知的世界》，本 CSV 未做合并。
- **封面图 URL 不在这份 CSV 里**：`database.txt` 的表头没有封面字段，
  所以没导出。需要封面可以另外提供（198 本里 158 本有真实封面 URL）。

## 数据怎么来的

采集自公开榜单页（Tavily 搜索 + 静态/动态解析）与起点中文网移动端、微信读书，
只取出版元数据（书名/作者/简介/封面/链接），**不采章节正文**。

重新生成：

```bash
python export_csv.py            # 只扫描噪声条目，导出 novels.csv
python export_csv.py --clean    # 先删噪声条目（媒体名/杂志名/作者名）再导出
```
